if (armanHigherBMI) {
    console.log(`Arman’s BMI (${armanBMI.toFixed(2)}) is higher than Kuat’s (${kuatsBMI.toFixed(2)})!`);
  } else {
    console.log(`Kuat’s BMI (${kuatsBMI.toFixed(2)}) is higher than Arman’s (${armanBMI.toFixed(2)})!`);
  }
  